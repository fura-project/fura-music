---
execution:
  mode: AUTONOMOUS_DEVELOPMENT
  work_domain: UI
  state: AWAITING_HUMAN_REVIEW
  acceptance_milestone: M1
  active_workstream: FIRST_PARTY_FLUTTER_LOCALIZATION
  current_task: HD_026_HUMAN_ACCEPTANCE
  next_action: HUMAN_LOCALIZATION_COPY_AND_VISUAL_REVIEW
---

# Current State

- **2026-09-12 HD-026 localization machine checkpoint:** official Flutter
  `gen_l10n` now owns an 843-message English template and matching Simplified
  Chinese candidate catalog. Settings schema v4 persists Follow system,
  English or Simplified Chinese; versions 1–3 and unknown locale values migrate
  safely to Follow system without discarding other settings. Locale changes
  preserve the selected Provider, authentication, loaded catalog/search state,
  Queue, current Track, playback stage and app-lifetime playback host. The
  first-party Flutter surfaces, failures, dialogs, snackbars, tooltips,
  accessibility labels and announcements use semantic ARB keys, while
  Provider-owned content and protocol values remain untranslated. QQ/NetEase ×
  English/Chinese desktop, compact, authentication, Settings, Search, Library,
  Home and expanded Now Playing candidates were rendered with a real CJK font
  and inspected for overflow; they remain review artifacts, not Human
  acceptance. Final gates pass: `flutter gen-l10n`, formatting of 246 Dart
  files, `dart analyze`, all 552 Flutter tests, the native Settings persistence
  integration test, Linux Release and Android ARM64 Release (44.4 MB APK).
  Native pre-`runApp` AudioService/window strings remain TD-012. Starting HEAD
  was `ca6d33ce3ead07ad6f16f099ce1ad9bbeb6d0964`; all changes remain local and
  unpushed. Simplified Chinese wording, visual rhythm and physical-device
  behavior remain `HUMAN_REVIEW`; no Human acceptance is claimed.

- **2026-09-12 Android system-playback ownership correction:** maintainer
  physical-device evidence showed that the prior registered Android media
  components did not make notification/system controls operate playback. The
  static registration checkpoint had missed a lifecycle defect: the actual
  Queue, Track playback controller and audio engine were created by
  `UserLibraryPage`, temporarily attached to `ProjectSystemAudioHandler`, and
  detached/disposed with that page. One root `AppPlaybackHost` now constructs
  and owns the Rust-backed Queue controller, media resolver, lyric controller
  and `audioplayers` engine for the application lifetime; the same controller
  is permanently held by the handler passed to `AudioService.init` before
  credential restoration. Product pages only observe/command it. Platform
  initialization failure preserves that exact controller as foreground-only
  playback instead of creating a second owner. This keeps `audio_service`
  responsible for Android Service/MediaSession/notification/media buttons and
  keeps the project handler limited to provider-neutral state/command mapping;
  no second playback plugin or Queue was added. A focused regression proves
  system Pause/Play/Next still reach the same session after a page listener is
  removed; all 528 Flutter tests, 237-file format, `dart analyze`, the Linux
  real session-bus system-playback integration, and an ARM64 Release APK build
  pass. The APK is 43,768,152 bytes, contains only `arm64-v8a`, retains the
  media foreground service/receiver and required permissions, and passes 16 KB
  zip alignment. ARM64 Release lint passes with 0 errors and the existing one
  Gradle-version warning. The current host has no Android device, so notification,
  lock-screen, headset, Activity/task removal and background-resume behavior
  remain `HUMAN_REVIEW`; process-death Queue restoration remains out of scope.
  Starting HEAD was `4c0cfad2ba7b694902ed0dc3b87fe36e8c8b96de`;
  the focused correction is retained in local commit `ca6d33c` and remains
  unpushed.

- **2026-09-12 HD-025 built-in Provider UI integration machine checkpoint:**
  the existing Material 3 product surfaces now serve QQ Music and NetEase Cloud
  Music through one persisted, QQ-default Provider selection. Settings schema
  v3 migrates legacy values, safely defaults unknown values and rolls back
  failed serialized writes. Bootstrap exposes both fixed Providers. Shared
  typed Bridge reads dispatch by exact Provider identity; QQ and NetEase have
  isolated vault keys, authentication/session owners and rejection cleanup.
  Startup restores only the selected Provider, while later selection lazily
  restores its stored credential. NetEase uses only Provider-default QR; QQ
  keeps QQ/WeChat QR and supported desktop Quick Login. Provider-scoped Home,
  Discover, Search, Library and detail state is replaced on selection, with
  old completion suppression and non-destructive QR/credential-verification
  cancellation. The Queue and current provider-owned Track remain mounted and
  exact media/lyrics/comments/MV/related routing remains independent of the
  Settings selection. NetEase hides Radar, cloud Recent Plays and remote writes;
  Daily Tracks and Personal FM retain truthful names. Pinned FRB 2.13.0
  generation passes with no deleted/orphaned generated API file. Final machine
  gates pass: Rust format, workspace and all-target tests at 535 passed / 0
  failed / 20 explicitly ignored, strict Clippy, 237-file Dart format, `dart
  analyze`, all 527 Flutter tests, Linux Release and Android ARM64 Release
  (43,768,152-byte APK). Required synthetic desktop/compact Settings, signed-out
  NetEase, Search and signed-in Library frames were rendered and inspected for
  overflow, wrong Provider copy and unsupported actions; aesthetic acceptance
  remains Human-only. Starting HEAD was
  `e1715ee7c5763d2fbcfd3df5d9bb4708f684be76`; logical commits remain local and
  no push is authorized. The exhaustive status is in
  [the HD-025 audit](docs/development/built-in-provider-ui-integration.md).

- **2026-09-11 Android system media and credential-transfer Go/No-Go
  checkpoint:** the QQ login dialog now presents desktop Quick login and QQ QR
  scan as two clearly labelled peer methods in a wide two-column layout, with a
  stacked compact fallback and retained QR live-region/control separation.
  Android did not lack a playback plugin: the existing `audio_service` /
  `audio_session` owner and `audioplayers` engine were already registered with
  the required media foreground service/receiver. The system-playback binding
  now uses an explicit monochrome notification icon, resumable notification
  click behavior and a foreground service that stays available across Pause,
  while the existing Queue controller remains the sole playback truth. A
  second playback plugin was deliberately not introduced. The current QQ
  credential's minimum reusable fields are `music_id`, `music_key` and a
  non-zero `login_type`; expiry and channel-specific identity/token fields
  remain optional but are carried when present. A Debug-only typed Rust Bridge
  harness exports an XChaCha20-Poly1305 encrypted bundle and separate
  single-use secret into new owner-private files, imports them only as an
  unverified candidate, and requires the normal QQ server verification before
  authentication. Dart never receives credential, ciphertext or key bytes;
  transfer files are bounded, time-limited, tamper-detected and cannot be
  overwritten. This completes only the machine-side experiment harness: no
  formal mobile pairing UI, LAN listener, naked credential QR or QQ Connect
  AppID impersonation was added. Real second-device restore and Android
  notification/lock-screen/headset controls remain explicit Human Go/No-Go
  checks. A later physical-device failure exposed the page-owned playback
  lifecycle defect; the 2026-09-12 ownership correction above supersedes this
  checkpoint's static system-control inference. Existing QQ recent-history
  read remains completed independently;
  recent-history write remains evidence-blocked. Final machine gates pass:
  235-file Dart format, `dart analyze`, all 513 Flutter tests, 528 passed / 20
  explicitly ignored Rust workspace/all-target tests, Rust format, strict Clippy, Linux Release,
  Android ARM64 Release (43.0 MB APK), and Android release lint (0 errors / 1
  toolchain-version warning). See
  [credential-transfer Go/No-Go](docs/research/qqmusic-credential-transfer-go-no-go.md)
  and [Android playback diagnosis](docs/research/android-playback-diagnosis.md).
  Included in the maintainer-requested local Git checkpoint; no push.

- **2026-09-11 shared collection detail and interaction pass:** Discover's
  new-Album/new-song filter Shells remain mounted while only their async content
  changes, preserving one compact native `SegmentedButton` and its Material
  selection animation without the prior duplicated/offset loading frame. A new
  scroll-observing collection layout gives Playlist, Album and Ranking the same
  continuous 132 px collapse: artwork contracts to 64 px, metadata tightens,
  and the headline hands off to a `titleLarge` Shell title/Back action. Album,
  Ranking and nested Artist routes now stay inside the retained Shell so the
  desktop sidebar, player, source destination and nested return stack survive
  every direct/nested Album route. The shared dense Track grammar now covers
  Liked, Playlists, Recent Plays, Radar, New songs, Albums, Rankings and Artists:
  pointer Play owns a Material state layer/tooltip, Artist and Album text are
  bounded independent actions, and multiple Artists use one adaptive chooser.
  Blank column space still activates the Track row, compact metadata ellipsizes,
  and existing row-to-row Tab/context-menu behavior is preserved. Shared state
  panels can scroll in unusually short Shell viewports. Targeted interaction,
  focus, navigation, adaptive and visual-fixture tests pass; final complete
  Flutter gates are recorded after this entry. Deterministic Discover,
  Playlist-collapse, Album and Ranking desktop renders remain Human-review
  candidates. Final gates pass: 235-file Dart format, `dart analyze`, all 512
  Flutter tests and Linux Release. See
  [Collection Detail Design](docs/design/collection-details.md) and
  [Discover design](docs/design/discover.md). No commit or push.

- **2026-09-11 Discover component/motion third pass:** the maintainer asked for
  recognizably standard Material 3 filter controls, a stronger animated
  collapsed title, a useful replacement for the Ranking artwork fade, and a
  truthful answer on shared Track lists. New-album and new-song filters remain
  the correct single-choice `SegmentedButton` primitive but now retain the
  standard selected check; peer Discover destinations correctly remain a
  secondary `TabBar`. The page heading and supporting copy continuously
  fade/contract while a bold `titleLarge` Shell title fades/slides into the top
  bar over the same 240 ms interval; search alignment and title spacing animate
  with it, while reduced-motion mode remains immediate. Ranking cards no longer
  bleach half the cover: intact artwork carries a bottom readability scrim and
  Track count, with title/period on the tonal information side. The repeated
  dense Track-row surface is now shared by Liked, ordinary playlists, Recent
  Plays, Radar and New songs; paging, search and page-specific actions remain
  with their existing owners. Targeted Discover, Recent Plays, Liked and
  playlist-detail tests pass, and deterministic desktop/compact plus
  mid-transition renders were regenerated for Human review. Final UI gates
  pass: 232-file Dart format, `dart analyze`, all 508 Flutter tests and Linux
  Release. The full run caught one provisional row-focus regression; the shared
  surface now preserves the prior row-to-row Tab sequence and the isolated
  regression plus the complete rerun pass. No commit or push.

- **2026-09-11 Discover utility-first second pass:** Human review rejected the first candidate's explanatory banners, nested filled surfaces and oversized media cards. The correction removes every section-lead panel, returns the title/tabs to the page canvas like Liked, and matches Home's 136--180 px media density with transparent normal cards. Rankings now use compact half-artwork cards whose cover fades into the period/count side. Radar and New songs reuse the Liked table header, 56/64 px transparent Track rows and contextual actions. New-album and new-song category controls are joined, horizontally scrollable segmented controls. Discover Radar now combines its bounded sparse-first-page fill with Liked's velocity/latency look-ahead: scrolling can request at most two serial service pages, upward movement cancels viewport demand, and append failures retain old rows. The current `newsong.NewSongServer/get_new_song_info` contract has no continuation fields, so New songs uses genuine lazy row construction but does not fake remote pagination. `dart analyze`, the 252-file Dart format gate, all 508 Flutter tests and Linux Release pass. Deterministic 1200x800 and 390x844 renders for all five sections were generated and inspected without overflow. Human aesthetics remain pending. See [Discover design](docs/design/discover.md). No commit or push.

- **2026-09-11 Discover Material 3 redesign candidate:** the maintainer redirected the active task to the five Discover tabs while preserving the existing Shell, player, navigation, data gateways and detail routes. Playlists and new Albums now use responsive filled media cards; Rankings use grouped chart cards; Radar and New songs use rounded continuous Track surfaces; every content tab has a low-emphasis section lead with restrained primary actions, while region/category choice chips remain horizontally scrollable. Home and Discover still own separate Radar controllers. The shared controller defaults to no prefetch, while Discover alone fills an upstream-sparse first page toward ten unique Tracks with at most two serial `hasMore` page requests; cancellation, account replacement, deduplication and append failure behavior are retained. The targeted controller and Discover interaction tests pass, `dart analyze` passes, and deterministic 1200x800 plus 390x844 renders for all five presentation grammars were generated and inspected without overflow. The candidate and its copy remain Human review; synthetic art/data do not establish live QQ density or aesthetic acceptance. See [Discover design](docs/design/discover.md). No commit or push.

- **2026-09-10 QQ Album-order/comment-compatibility regression repair:** the reported Album Track mismatch was reproduced below Flutter: `AlbumSongList/GetAlbumSongList` was relying on QQ's mutable default because it omitted the explicit canonical selector. A bounded public comparison returned Album indexes 13→1 without it and 1→13 with `albumID=0, order=2`; production now sends those parameters, preserves the upstream order, and has an opt-in public live order regression. The comment failure combined three live-observed legacy shapes: stale-low `commenttotal`, replies whose matching body exists only under `middlecommentcontent`, and unavailable rows without displayable content/author. The Rust mapper now selects the nested body by exact opaque comment ID, omits only unavailable display rows while retaining raw cursor progress, and normalizes stale totals with full-page continuation/short-page termination; even a fully omitted raw page remains able to advance. Flutter's typed adapter still rejects impossible bounds but no longer equates display-row count with raw terminal cursor position. The modern `CommentRead/GetNewCommentList` cursor route was investigated but not substituted into the current stateless offset contract. Anonymous live gates pass for one canonical public Album and three legacy comment pages without retaining names, comment text, authors, identities, or response bodies. Final gates pass: 515 offline Rust tests / 19 ignored live-Human tests, strict workspace/all-target Clippy, Rust format, `dart analyze`, 252-file Dart format, all 503 Flutter tests, and Linux Release. Exact maintainer-reported Albums/songs and official-client visual parity remain Human review. See the [specialized investigation](docs/research/qqmusic-album-comments-regression.md). No commit or push.

- **2026-09-09 HD-024 NetEase hardening/read-parity checkpoint:** execution is explicitly restored to `AUTONOMOUS_DEVELOPMENT / CORE`; retained UI candidates and the existing QQ/Flutter working-tree edits were not changed. Workspace metadata now names the canonical `fura-project/fura-music` repository. Native media and every future native NetEase edge share one private process-level Provider owner. QR-confirmed credentials survive transient account verification only as non-exportable/non-authenticated pending candidates, with rejection/sign-out/replacement and late-completion tests. Public Playlist/liked identities and whole Albums now support deterministic windows above 1,000 under 2 MiB-derived memory caps. Comments, exact-seed related Tracks, exact-category new songs/new Albums and Track-associated MV are implemented through existing neutral contracts; one eight-request anonymous parity window and one three-request >1,000 Playlist window passed without a risk STOP. Final Rust workspace and all-target gates each pass 509 tests with 17 explicit live/Human tests ignored; strict Clippy and Rust format pass. The retained Flutter tree passes analysis, all 499 tests and the 252-file format gate; Linux Release and ARM64 Android debug builds pass. The new 72-request-maximum Human account-read matrix is compiled, ignored and intentionally unrun. Windows/macOS/iOS builds, actual account reads and authenticated media remain Human/environment evidence; NetEase recent history remains externally evidence-blocked. See the [final audit](docs/research/netease-remaining-work-audit.md). No push.

- **2026-09-09 Human confirms read recovery and requests three UI corrections:** the maintainer reports “可以了” and supplies a Recent Plays screen with 500 loaded records. This establishes a successful maintainer-operated read in the tested account context; it does not independently verify both login channels, ongoing cross-client ordering, or outbound history reporting. Mode remains `HUMAN_GATED_REGRESSION`; domain is now `UI`. Authorized scope is only monochrome Recent Plays rows, a scroll-collapsing title in the existing top-search slot, and consistent Settings/playlist canvas colors while preserving opaque transition occlusion. The Agent consulted `agy` / Gemini 3.8 Flash (High) in three isolated, sanitized MD3 questions; only abstract component/layout descriptions were sent. No account or screenshot content was shared. The candidate uses one lazy scroll view, measured title handoff, pinned controls, search-focus protection and a 12 px return threshold buffer. Normal rows use the existing canvas; Settings/playlist backing remains opaque using the same canvas. `dart analyze` and changed-file formatting pass; the complete 79-test Shell/widget suite and 38 targeted paging/search/recent regressions pass, including the added boundary and focus checks. Seven canonical render checks pass; desktop/compact, dark/reduced-motion and Settings/playlist entry frames were inspected without overflow or content bleed through the backing. Linux Release builds; the isolated review launcher `/tmp/fura-ui-review-002dr3b6/run.sh` passes syntax and dynamic-library checks. The native bundle includes the corrected cloud-read route. This batch does not change Rust/Bridge behavior, run an account or self-accept aesthetics. Starting HEAD is `c8eb17252a171acb1a37a35a1b7c7681aeaf7353`; the five existing modified QQ/Core/evidence files are preserved.

- **2026-09-09 corrected recent-history candidate:** Human confirms QQ and WeChat both return method code 500003. Agent static inspection of the checksum-verified official Windows 22.22 installer found the real route `music.musicasset.PlayRecentlyRead / GetPlayRecentlyInfo`, integer `type=2 / updateTime=0`, and `data.data.songList[].track/lastTime`. This supersedes the wrapper's `RecentPlayList.GetRecentPlayList` and `begin/num/vecPlayRecord` assumptions. One credential-free request to the real route returned HTTP 200 / outer 0 / method 1000; no account read was automated. Rust now requests and decodes the official snapshot, with 8 MiB / 5,000-record local limits and 1–100-row local pages. The snapshot belongs to the exact authenticated state and is dropped on sign-out/rejection/replacement; later pages reuse it, while refresh and in-flight replacement use an exact request token. Existing UI and provider-neutral boundaries remain unchanged. Nine Client and seven Provider recent-history regressions pass. Final `cargo test --workspace --all-targets`: **491 passed / 0 failed / 14 ignored**; `cargo fmt --all -- --check`, strict workspace/all-target Clippy and native Bridge build pass. The isolated launcher is `/tmp/fura-qq-recent-fixed-lwqdf_gi/run.sh`; its copied native library matches the new build SHA-256 `f320503b3e2d0e00a22ba73e7069956d42f9c261415d8d90bf716193aa072851` and its script passes `bash -n`. The Agent did not launch the app or access an account. Successful real-account reads still require Human retest. Protocol research was performed by the Agent; no Human interception certificates, cookie export or reverse engineering is required. This follow-up began at HEAD `c8eb17252a171acb1a37a35a1b7c7681aeaf7353` with the four prior diagnostic files modified; those changes are retained and the Client export is the only additional modified file. See [direct protocol evidence](docs/research/qqmusic-recent-plays-evidence.md). No push.

- **2026-09-09 Human regression redirection:** the maintainer explicitly selects `HUMAN_GATED_REGRESSION / CORE` and reports that signed-in QQ Recent Plays shows a read failure. Investigate this specific request/decoder/error path and fix only evidenced defects. NetEase Core remains at its previous machine checkpoint with account evidence pending; no continued NetEase development or UI visual iteration is authorized by this regression. Start HEAD `c8eb17252a171acb1a37a35a1b7c7681aeaf7353`, clean working tree. The maintainer confirmed WeChat QR login. Static/FakeTransport checks preserve the correct WeChat channel and account cookie; the reference implementation warns of possible WeChat incompatibility but does not prove a restriction. An explicit opt-in, redacted Client diagnostic and isolated native diagnostic bundle are ready for one Human retry. The Human retry returned outer code 0 / method code 500003. This reproduces upstream-method failure before record mapping; credential expiry, empty history and a WeChat-only restriction are not established. The exact current Windows request is needed for a protocol repair. The 500003 regression preserves the current session and sends no automatic second request. Read acceptance is failed for this WeChat context; sync is not fixed. Nine Client and two Provider targeted regressions pass; full Rust validation is 486 passed / 0 failed / 14 ignored, with fmt, strict Clippy and native library build passing. No public Bridge/Flutter change, commit or push belongs to this regression.

- **2026-09-09 NetEase machine verification complete:** `netease-client` and `provider-netease` implement all four Search types, exact Track detail, bounded public playlist/Album/Artist browsing, line lyrics, rankings/public recommendations and ordinary standard media. Thirteen anonymous capability observations passed under a serial, ignored, 16-request live budget; no account or media download was used. The authenticated Core foundation includes opaque QR/PNG/cancellation, provider-owned versioned credential import/export/verified restore, account summary, bounded user/owned/private/liked/favorite collections, daily Tracks, Personal FM, personalized playlists and authenticated standard media. Synthetic tests cover explicit rejection, deadline, late completion, replacement and between-request cancellation. Ranking pages preserve raw cursor/omissions even for an unavailable window. Provider API adds only exact Track details, daily Track batches, native QR selection and the static QQ/NetEase enum/resolver pair. Private native media composition initializes NetEase only on its exact route; public Bridge APIs, generated Dart and Flutter are unchanged. Final offline Rust gates: 482 passed / 0 failed / 14 ignored, workspace/all-target tests, fmt and strict Clippy; pinned FRB 2.13.0 generation and API orphan audit pass. These machine checks do not establish real-account behavior or audio playback. See the exhaustive [Remaining Work Audit](docs/research/netease-remaining-work-audit.md), [protocol evidence](docs/research/netease-protocol-evidence.md), and [auth evidence](docs/research/netease-auth-evidence.md). All safe authorized implementation is complete; only the listed real-account/entitlement/environment observations remain `HUMAN_EVIDENCE_REQUIRED`. That checkpoint used autonomous CORE; current scheduling is the Human QQ regression above. Current UI candidates remain retained and visually unreviewed. No push.

- The HD-023 implementation commits through `c8eb172` are present on canonical remote `main`. HD-024 hardening/parity commits remain local and unpushed as required.

- **2026-09-09 Human redirection (HD-023):** persist `AUTONOMOUS_DEVELOPMENT / CORE`; implement NetEase as the second built-in formal Provider while QQ remains first-class. Current UI candidates are retained with Human visual review deferred (neither accepted nor rejected). Flutter and visual iteration are frozen. Start: HEAD `317666a9913e91f13df5d8bf0bba10f4aee79a43`, clean working tree; `git status` and 30 commits inspected and captured in `/tmp/fura-netease-evidence/initial-git.txt`. Execute anonymous catalog/lyrics/media first, then evidence-backed authenticated foundations; real account evidence remains Human-operated. Local logical commits only, no push.

- The maintainer reported three exact UI regressions without reopening the accepted Shell, playlist, pagination, or Home composition. The extended sidebar now derives a personal-playlist selection from the active typed `PlaylistLocalRoute`: opening a sidebar playlist removes the stale Liked indicator and applies the existing selected-container grammar to that exact playlist row. The retained Shell transition now gives both Settings and playlist details their own fully opaque moving `surface`, while fading only page contents, so a playlist cannot paint text or artwork over the previous page before its background arrives. Home's Spotlight keeps the Hero Material/card geometry stationary and switches only its internal artwork/text scene; Next sends the outgoing scene left and receives the new scene from the right, while Previous reverses both directions. A 3 px in-card progress indicator follows the same 12-second rotation interval, resets with manual navigation/lifecycle changes, stops while auto-rotation is paused, and remains absent when reduced motion disables auto-rotation. Focus and input ownership remain unchanged. Targeted transition assertions cover selected state, an opaque 120 ms entry frame, stable Hero bounds, paired directions, and paused progress. `dart analyze`, the full 232-file Flutter Dart source format gate, all 498 Flutter tests, and Linux Release pass. Inspected temporary 1440×900 transition, 1440×960 Home, and 390×844 Home/playlist renders show no overflow or background flash; aesthetics and perceived motion remain Human review. The maintainer authorized this candidate for the accumulated Git checkpoint and push; visual acceptance remains separately Human-gated.

- QQ cloud recent-play **read** now has a production path through the existing architecture: the signed `RecentPlayList/GetRecentPlayList(begin,num,uin)` request stays inside `qqmusic-client`, `QqMusicProvider` reuses the sole credential generation and declares only `RecentHistoryRead`, the typed cancellable Bridge reuses the existing Track-page DTO, and production `main.dart` injects `RustRecentPlaysGateway` into the already-complete Recent UI. The evidenced key-derived `g_tk` fields and `zzc` body signature have a fixed known-answer request test. The existing `PagedTracksController` remains the only pagination/search/refresh/prefetch owner. Pages are bounded to 100; an absent upstream total becomes a safe lower bound until a short terminal page, and invalid rows advance the raw cursor as omissions. A `songmid`-only source identity remains playable without fabricating numeric QQ IDs, while numeric-ID-dependent mutations stay rejected. Valid empty, auth rejection, service failure, malformed response and account replacement remain distinct; explicit credential rejection clears the persisted credential, while transient refresh failures retain the UI snapshot. Request, mapping, empty/paging, rejection, malformed/missing-field, Bridge cancellation, Gateway cleanup and unknown-total presentation tests pass. The full gates are 436 offline Rust tests, strict workspace/all-target Clippy, Rust format, `dart analyze`, 252-file Dart format, all 498 Flutter tests and Linux Release. Twelve explicit live/Human Rust tests remain ignored. No stored account was automated, so same-account order, actual multi-page behavior, QQ-versus-WeChat support and real artwork remain Human acceptance. Public-code and official-IoT research still yields no ordinary QQ-session write protocol with a closed host/module/method/parameter/timing contract; `RecentHistoryWrite` is deliberately not advertised, no playback lifecycle hook was added, official-client visibility is unverified, and bidirectional sync remains incomplete. See `docs/research/qqmusic-recent-plays-evidence.md`. No commit or push belongs to this batch.

- The maintainer explicitly requested Human-gated sidebar/recent-plays work (HD-018). Signed-out desktop, rail and compact navigation now hide MY MUSIC/Liked/recent plays/personal playlists while public browsing and sign-in remain available. The new retained Material 3 recent-plays destination uses a shared `PagedTracksController`: the former playlist controller is now an identity adapter over the same serial page pump, retaining bounded scroll prefetch, incremental full search, retry cadence and stale-result suppression. **The recent-plays page is UI foundation only: no production QQ cloud-history read/report adapter exists, same-account cross-client synchronization is not implemented, and production shows an explicit not-connected state with no count or active data actions.** Session-only Home seeds are never substituted. Tests use a synthetic 250-row source only. Cloud evidence and the current blocker are recorded in `docs/research/qqmusic-recent-plays-evidence.md`; design and acceptance are recorded in `docs/design/recent-plays.md`. The new sidebar entry exposed spatial focus interleaving with playlist rows; a sidebar/rail traversal group restores the original consecutive-row keyboard test without relaxing its assertions. Review renders use loaded CJK/icon fonts; compact loading status is placed above the list to clear the floating player. Dart analysis, formatting of all 231 Dart files, all 495 Flutter tests, the original desktop keyboard traversal test, four dedicated recent/guest Widget scenarios and the Linux Release build pass. Inspected 1440×960 and 390×844 synthetic list renders plus 1440/900/390 px unavailable and guest states show no overflow; cloud data, real artwork and Human visual acceptance remain unverified. Prior Home/playlist candidates remain unaccepted. No Rust/Bridge file, stored account, real history response, remote write, commit or push belongs to this batch.

- The maintainer redirected the Human-gated batch to Home recommendations. Research and implementation are recorded in `docs/research/home-recommendation-strategy.md`. Signed-in spotlight now uses the existing authenticated personalized-playlist feed and preserves its initial ranking; guest spotlight remains explicitly public. The personal shelf replaces the incorrect Liked CTA with Refresh and displays its entire bounded result on one lazy horizontal row, including desktop scrollbar/arrows when needed. Home-visible/foreground 15-minute freshness checks and coalesced manual refresh are separate from the 12-second carousel; Discover owns an independent public controller so Home refresh cannot reset its pagination. Transient account-resource failures retain the last snapshot with an explicit warning, while rejected credentials clear the affected private result. Rust Domain now owns a bounded, recency-weighted recommendation seed pool from actual playback observations (30 seconds or half a short Track); selecting, buffering, pausing, and seek jumps do not qualify. Related picks stay stable through current-Track changes, and the first personalized Track is no longer treated as history. The pool is session-only, not persisted/imported/uploaded, and is rebuilt across account/Shell lifetimes. Dart analyze, 492 Flutter tests, 428 offline Rust tests, Rust format/strict workspace Clippy, and Linux Release pass. Inspected synthetic 1440×960, 1000×900 and 390×844 renders cover account spotlight, desktop overflow/last-card reachability, and related-song sections with loaded Latin/icon fonts; CJK fallback/real artwork and device acceptance remain Human evidence. No stored credential, real account response, cloud history, remote mutation, commit or push was part of this batch. The preceding playlist candidate remains available for maintainer scroll/search retest; this Home batch does not self-accept it or the new recommendation quality.
- The maintainer authorized smoother up/down playlist loading. Liked Songs and ordinary playlist detail now share a presentation-only scroll predictor with a 50–200-row lookahead based on scroll speed and successful-page latency, replacing their separate 720 px end triggers. One controller pump owns manual append, bounded one/two-page prefetch, and full-search draining: search adopts an active page, repeated actions cannot skip active cadence/backoff, upward scrolling withdraws pending browse demand without evicting loaded rows, and refresh/disposal suppress stale work. Short/duplicate/omitted pages consume the browse budget, so one scroll cannot drain an entire collection. Existing lazy builders, scroll storage, layout, QQ's 100-row request bound, raw continuation cursor, search index, and account boundaries remain unchanged. Dart format/analyze, all 488 Flutter tests (17 added this pass), and Linux Release pass. Inspected 1440×720 desktop-collapsed and 390×844 compact expanded/collapsed synthetic renders retain the header/list/player geometry without overflow; their default test glyphs do not establish actual typography or visual acceptance. No Rust/Bridge code changed in this pass. No stored credential or real account response was accessed, and no commit/push was made; authenticated latency and perceived scrolling remain Human-review evidence.
- The maintainer's follow-up authenticated Liked search reached all 1,032 reported rows with one coarse omitted-row count, closing the earlier post-300 pagination defect while revealing two narrower issues: ten remaining 100-row requests still felt slow, and a one-character Latin misspelling could not match the intended Track because the UI used only lowercase substring comparison. The updated candidate keeps the QQ-evidenced 100-row request bound but publishes matches after every page, reduces the successful-page cadence from 500 ms to 180 ms, retains a 500 ms recovery cadence plus finite one-second/three-second backoff after transient failure, and never overlaps two page requests. A page-local incremental index normalizes case, full-width forms, punctuation, common Latin accents, and cross-field title/Artist/Album terms; exact matches rank first and Latin tokens of four or more characters receive bounded one- or two-edit typo tolerance, while short/CJK queries remain exact to avoid false positives. The UI labels approximate results and live checked/match counts. Dart formatting/analyze, all 471 Flutter tests, and the Linux Release build pass; the prior 422-test Rust format/Clippy gate remains current because this follow-up changed no Rust or Bridge code. No stored credential or account response was read by the Agent; perceived speed and real result quality remain pending maintainer review.
- The maintainer requested repair of the concrete code and scheduling drift found in the current accumulated candidate. Liked Songs now listens to a distinct current-Track identity signal instead of rebuilding from playback position, state, volume, or lyric ticks; filtered Track results are reused until either the loaded snapshot or query changes. App Settings mutations are serialized at the storage edge, while a focused controller owns optimistic state and rolls consecutive failures back to the last successfully persisted document rather than another unpersisted request. Playback-quality mapping, labels, fallback comparison, tooltip, and feedback copy now have one shared semantic owner. The canonical 1,029-Track fixture retains its truthful total and supplies bounded continuation pages instead of terminating by shrinking the collection to the seven visual rows. Dart formatting/analyze, all 459 Flutter tests, the Linux Release build, all 420 Rust workspace tests, Rust formatting, and strict workspace/all-target Clippy pass. No commit, stored-account access, or live QQ request was part of this repair.
- The earlier pagination candidate changed the 100-row Core bound into a page size, added near-boundary loading and a sequential cancellable Liked-search drain, and proved only that one anonymous public playlist accepted offset 300. Its offline tests and public probe did not establish authenticated Liked behavior; the subsequent Human result recorded above demonstrated that its immediate stop/retry policy and usable-row-derived cursor were insufficient.
- The maintainer reported one exact Track as source-unavailable in Fura and then confirmed that the same account plays its full duration in official QQ Music. A content-bounded public reproduction found that F000/M800/M500/C400/C200, both `UrlGetVkey` and `CgiGetVkey`, and all three previously evaluated filename identities consistently returned per-item `104003`, so the defect is not a missing quality or filename permutation; public metadata separately exposed only a 60-second trial. Because the official signed-in comparison establishes a real authenticated compatibility gap, Fura now retains `UrlGetVkey` first but makes one same-profile desktop-compatible `CgiGetVkey` request with the existing credential only after exact `104003`. Other unavailable codes do not rotate routes; every await rechecks the sole session owner, and credential, replacement, network, service, rate/risk, and malformed outcomes STOP. Offline request-shape, order, redaction, success, service-STOP, and three-await replacement regressions pass, along with all 420 Rust workspace tests, strict workspace/all-target Clippy, formatting, and a fresh Linux Release build. The exact real-account Track still requires a maintainer-operated retest; no stored credential, source URL, or account content was read.
- The maintainer requested a QQ Music-style Standard/HQ/SQ selector directly on the shared playback surface and reported that ordinary authenticated playback now succeeds after the media-identity correction. The compact, desktop, and Expanded Now Playing bars expose one persisted `STD`/`HQ`/`SQ` menu without changing their accepted heights. Active selection re-resolves the exact current Track while retaining approximate position and paused/playing state, then reports the actual quality or fallback through a content-free snackbar/tooltip. The Core adds the independently corroborated F000 FLAC profile ahead of the existing M800→M500→C200 ladder only for explicit SQ preference; every fallback still requires typed per-item unavailability, while network, credential, service, malformed, and replacement outcomes STOP. Generated Bridge types remain provider-neutral and report the actual format/quality. Rust workspace/all-target tests and strict Clippy, Dart analyze, all 447 Flutter tests, 67 focused playback/settings tests, inspected compact/desktop renders, and packaged Linux MP3/M4A/FLAC playback integrations pass. This does not prove which authenticated profile the maintainer's account receives; exact SQ/HQ/fallback selection remains the next Human observation, and no stored credential or media URL was accessed.
- The maintainer reported three Settings Shell regressions in the pending visual candidate. The Settings detail transition now moves an opaque `surface` page layer while fading only its contents, so the retained Home/Discover/Liked body cannot show through during entry or exit. The medium-width music Rail keeps Settings bottom-pinned without the extended-sidebar divider, matching its single compact navigation context. At the extended-sidebar breakpoint, sign-in/sign-out is now the trailing action in the top-left identity row; the duplicate top-right account action is absent, while medium and compact layouts retain their reachable top-bar account action. Settings still hides every account action while open, and sign-out retains its existing confirmation and failure handling. Targeted transition/account/Rail Widget checks, inspected 1440×900 transition/final and 960×800 Rail renders, Dart analyze, all 443 Flutter tests, and Linux Release pass; perceptual transition quality and placement remain Human-review evidence rather than self-accepted UI.
- The maintainer redirected the Human-gated UI candidate to the compact player backdrop and the temporary Discover composition. At widths below 640 px, the shared player now overlays the retained Shell body while the 72 px NavigationBar remains the sole bottom-navigation owner, so the player capsule's outer insets reveal the scrolling page instead of a second solid bottom surface; the Shell wrapper remains structurally stable across resize to avoid remounting active detail routes. Discover retains its five real lazy-loaded sections and their loading/empty/error/pagination behavior, but now uses a Liked-aligned large title, horizontally scrollable Material secondary tabs, a compact tabs-only state whose title moves into the Shell top bar after scrolling, and one generic QQ Music description instead of repeated per-section headings. Recommended Playlists use two whole-card tap targets per row on compact and a bounded responsive desktop grid; other content types retain their appropriate chart, Track, Album, and filter layouts. A bounded local AGY/Gemini MD3 review informed height, touch-target, Surface, and bottom-clearance checks without changing the maintainer-owned composition. Canonical 390×844 expanded/collapsed and 1200×800 renders show no overflow and confirm page content behind the player insets. Dart analyze, all 443 Flutter tests, and Linux Release pass; maintainer visual/runtime acceptance remains pending, and no live account data was accessed.
- The maintainer requested removal of the redundant visible Library root now that Liked owns the supported saved-collection experience. The fourth retained destination now renders as `喜欢`/Liked on compact, rail, and Home entry points and always resets to the Liked root; the extended sidebar keeps one Liked item and direct playlist shortcuts but no Library item. Internal legacy section/controller code remains to avoid destructive data-layer removal, while the old favorite-Artist root is deliberately no longer loaded or exposed by this navigation pass. If QQ Music omits its built-in liked-songs identity, the Liked page retains its truthful tabs, shows Songs as unavailable, and keeps Playlists/Albums reachable. Liked collection scrolling now moves the live compact header into the Shell's topmost application-bar position rather than merely shrinking it inside the page: desktop title/tabs/actions/search/account action share one row, while compact title/search/Shell actions share the first row with tabs/actions below. Explicit return-scroll tracking prevents the Shell relayout from reopening the large header by itself. Medium-width Settings is now a bottom-pinned one-item Material `NavigationRail`, matching the other destinations' icon, label, state-layer, and indicator grammar instead of presenting a bare gear. Reduced motion swaps immediately. A stable Shell base wrapper preserves selected Liked tab, grid position, and focus across compact playlist details. Updated medium-rail and compact/desktop collapsed renders have no overflow. Dart format/analyze, all 442 Flutter tests, and Linux Release pass; maintainer visual/runtime acceptance remains pending. See `docs/design/liked-songs.md` and `docs/design/settings-shell.md`.
- The maintainer requested one coherent persistent playback bar across Home, Discover, Search, Liked, Settings, and retained details. The first current Track reveals the bar through a bounded Material-emphasized size/fade/upward transition on both compact and desktop layouts, with immediate reduced-motion behavior and no reanimation for later Track changes within the retained owner. Following two bounded local AGY/Gemini Material reviews, compact pages now share one 68 px `surfaceContainerHigh` player with 28 px corners, no outline or shadow, 12 px horizontal floating insets, artwork/identity, a filled primary action, and Queue above one fixed 72 px primary NavigationBar; Settings suppresses the primary navigation without recreating the player. Mobile artwork, identity, and remaining card surface open Expanded Now Playing while playback/Queue controls consume their own taps. Desktop artwork and Track information now form one focusable/clickable identity destination into Expanded Now Playing. Album/credited-Artist catalog navigation remains on the Expanded Now Playing Artist line, retaining direct single-destination dispatch, adaptive multi-destination selection, and exact Track/index stale-selection protection. Updated compact Liked and desktop persistent-player renders have no overflow. The 37-test playback flow, full 442-test Flutter suite, and Linux Release pass; maintainer visual/runtime acceptance remains pending. See `docs/design/now-playing.md`.
- GitHub Actions run `33762939140`, job `100673500253`, failed only the Flutter test step at commit `dc309f1`: 434 tests passed and the Home unavailable-Daily test failed. The test hard-coded `Public hero pick` as the daily spotlight even though production intentionally selects one of the two public candidates by calendar day; the 2026-09-03 CI day selected the other candidate, while the same test passed on 2026-09-04. The assertion now verifies that exactly one of the two truthful public candidates occupies the spotlight and retains the independent Daily/Radar geometry checks. No production Home behavior changed.
- The maintainer redirected the Human-gated UI task to make Settings a complete Shell state. At the extended desktop breakpoint, the existing left navigation slot now animates from music identity/destinations/playlists into a Settings-owned Back header plus truthful Appearance and Playback destinations; the rail breakpoint uses the same Settings navigation model. The top QQ Music search animates into live `Search settings`, and all sign-in/sign-out affordances are absent while Settings is open, including for signed-out users. Compact Settings now follows the two-level Stitch reference: a searchable Appearance/Playback category list opens one matching control page, and toolbar/system Back returns to the list before leaving Settings. Only the two settings with real persistence and behavior are exposed; unsupported Stitch account, download, lyric, privacy, gesture, update, and sign-out rows were not copied as placeholders. The retained main content, persistent player, and return focus ownership are unchanged. Main content, sidebar/rail, top bar, desktop section results, and compact hierarchy have symmetric Material-emphasized entry/exit behavior with reduced-motion handling. Targeted extended/rail, signed-out, compact hierarchy/search/back, persistence, mid-transition, 360 px, and reduced-motion Widget checks pass; inspected 1440×900 and 390×844 candidates have no overflow. Dart format/analyze and Linux Release pass. The complete Flutter suite now passes all 441 tests after removing the date-sensitive Home fixture assumption. See `docs/design/settings-shell.md`; Human visual/runtime acceptance remains required.
- The maintainer clarified that desktop QQ authorization means QQ Music-style local quick login: retain the initial QQ/WeChat choice, then on QQ show the QR beside accounts already signed in to desktop QQ and request a one-time ticket only after account selection. The earlier generic HTTPS handoff candidate was removed. A Human test reported no usable authorization after five seconds; the first candidate unnecessarily waited for up to ten serial 30-second avatar lookups before exposing the account list and also started discovery/QR merely by opening the dialog. The corrected candidate performs no work before the explicit QQ choice, returns the local nickname/masked hint immediately after the bounded odd-port probe, uses a non-blocking generic account icon, and defers real-avatar work until it can be made lazy without exposing the raw QQ identifier. The Core pins `localhost.ptlogin2.qq.com` to `127.0.0.1` and reuses the existing QQ Connect → QQ Music exchange after `pt_get_st` and `jump`. Absence of desktop QQ leaves QQ/WeChat QR available. Pinned Bridge generation, 412 Rust offline tests, workspace/all-target strict Clippy, Dart format/analyze, 32 targeted authentication tests, the focused two-stage desktop Widget regression, and Linux Release pass. The complete Flutter suite retains 434 passes plus the independently reproduced pre-existing Home geometry failure. The agent inspected only the current public login script, local listen port, and TLS certificate; it did not call the local account-list endpoint or authorize the maintainer's account. HD-022 and `docs/development/platform-native-authorization-plan.md` record the exact Human test and the separately blocked Android/iOS native-client scope.
- On 2026-09-11, with the maintainer's explicit authorization and desktop QQ already signed in, a content-free Linux live test completed the sole-account local quick-login chain through `pt_get_st`, `jump`, `check_sig`, QQ Connect authorization, and QQ Music credential exchange. The earlier Core candidate had three concrete protocol-drift defects: its 500 ms serial loopback timeout raced the current client's normal response latency; it accepted only legacy direct JSONP rather than the current fixed JavaScript assignment wrappers; and it incorrectly computed `pt_local_tk` with the QQ Connect `g_tk` seed 5381 instead of the current local script's zero seed. The wrong token sent `jump` down a fallback route without `p_skey`, while the current successful route requires a validated `ssl.ptlogin2.graph.qq.com/check_sig` step. Discovery is now staggered/concurrent with a three-second per-probe bound, both current and legacy wrappers are parsed without evaluating JavaScript, the zero-seed token is used, and `check_sig` is accepted only for the selected account plus expected QQ/Connect application identities. Synthetic redaction tests, nine focused quick-login tests, full Rust workspace/all-target tests, strict workspace/all-target Clippy, Rust format, Dart analyze, all 503 Flutter tests, and Linux Release pass. The live test printed and persisted no account identity, nickname, ticket, cookie, signed URL, OAuth code, credential, or response body. Visible Fura dialog completion, secure-vault restore after a clean restart, QR fallbacks, and Windows/macOS behavior remain Human-review evidence; no commit or push was made.
- The maintainer's content-free rerun localized the remaining reported lyrics failure to `original.invalid_xml`. Because the public Track title itself contains an ampersand, this is consistent with QQ's XML-shaped QRC envelope carrying an unescaped reserved character inside `LyricContent`; active independent parser evidence also extracts that field without requiring lyric text to be strict XML. The Client retains structural XML parsing but applies a bounded QQ-specific entity decoder only to the raw content attribute, decoding valid entities while preserving literal/unknown ampersands. Two focused fixtures reproduce the ampersand and unknown-entity behavior without adding a parallel XML parser. Rust format, 406 offline tests, and strict workspace Clippy pass. A maintainer rerun remains required because no real lyric body was captured or persisted; endpoint rotation remains inappropriate while the current Primary successfully returns and decrypts the document.
- The reported QQ lyrics compatibility failure exposed two overly strict decoder assumptions: the Client rejected encrypted line-timed (`qrc=0`) originals even though they remain synchronized lyrics, and any malformed optional translation/romanization payload discarded an otherwise valid original lyric. The bounded fix now preserves line timing without fabricating word segments, parses auxiliary LRC or QRC independently, and omits only a malformed optional track. `FURA_QQ_LYRIC_DEBUG=1` provides content-free stage codes for a maintainer rerun without printing Track identity, lyric text, credentials, ciphertext, response bodies, or media URLs. Rust format, 404 offline tests, and strict workspace Clippy pass; the one-request anonymous QRC live gate also passes. A maintainer rerun of the reported `Up&Up` case remains required because that exact catalog response was not captured, and a numeric-song-ID or alternate-endpoint route will not be added without evidence that the remaining failure occurs before decoding.
- The owned/favorite Playlist artwork regression is fixed and machine-verified. The shared optional-artwork normalizer introduced in `b9de860` accepted existing HTTPS values but upgraded cleartext artwork only for `qpic.y.qq.com`; QQ account Playlist payloads can also use the independently evidenced `p.qpic.cn` and `y.gtimg.cn` image hosts, so those otherwise valid URLs were reduced to absent artwork before reaching Domain/Flutter. The bounded policy now upgrades exact `qpic.y.qq.com`, `p.qpic.cn`, and `y.gtimg.cn` HTTP hosts, accepts protocol-relative URLs only for those exact hosts, and continues rejecting arbitrary cleartext, lookalike, credential-bearing, or malformed URLs. The owned-Playlist decoder regression now uses a real-shape `p.qpic.cn` URL. Rust format, 400 offline tests, and strict workspace Clippy pass; a maintainer refresh/relaunch remains required to confirm real account covers render again.
- The QQ capability-local protocol optimization pass completed its safe machine-verifiable scope under `AUTONOMOUS_DEVELOPMENT`. HD-021 now permits autonomous promotion only when exact static evidence, deterministic fixtures, bounded semantics, and relevant anonymous read-only live evidence agree. The existing `CgiGetDiss` strategy is now the deterministic anonymous `PRIMARY` for public `catalog:*` Playlist Detail: it omits Cookie/account fields even while signed in, while account-owned/favorite/liked contexts retain the sole authenticated credential owner. A two-request serial ignored live gate passed without recording content. Anonymous credential-like codes no longer masquerade as credential rejection, and code `2001` is a rate-limit STOP. Search Mobile/Lite/Adaptor/legacy and `CgiGetVkey` remain `EVIDENCE_ONLY`; Search was not re-probed after the existing `2001`, and the Media candidate was not probed without a demonstrated compatibility gap. No production protocol fallback was added, so there is no rotation path or unused session-guard framework; every current route performs one deterministic protocol request and STOP cannot issue a second endpoint request. Rust format, 400 offline tests, and workspace/all-target strict Clippy pass; 9 opt-in live tests remain ignored by default. provider-api, Bridge, Flutter, Domain, credential ownership, and Media Source separation are unchanged. Remaining strategy promotion evidence is authenticated/VIP/region/device or otherwise Human-operated Level 3 evidence.
- The maintainer-requested Home recommendation consolidation is implemented as a Human-review candidate. The top public recommendation now starts from a date-stable real public Playlist and rotates every 12 seconds with previous/next and pause/resume controls; reduced-motion keeps manual navigation but removes automatic rotation. Daily 30, Radar, personalized Playlists, personalized Tracks, public Playlists, latest new songs, and current-Track related songs remain separate truthful slots. Authenticated Home now loads the existing public `Latest` new-song contract as its own `Fresh releases` section without replacing personalized Tracks; its controller remains independent from Discover's regional new-song state. Synthetic 1440×960 and 390×844 renders show all supported sections without overflow. Dart format/analyze and all 431 Flutter tests pass; real QQ recommendation availability/quality and visual acceptance remain maintainer evidence.
- A bounded audit now distinguishes legitimate empty QQ reads, authorization/service failures, and incompatible response data across lyrics, comments, Track-associated MV, playlist/ranking artwork, related Tracks, Search, guest media, other public catalog, and account/library reads. Signed-out lyrics now use the evidenced anonymous no-Cookie request instead of failing before transport. Optional playlist/ranking artwork shares one bounded HTTPS policy for exact evidenced QQ image hosts, while invalid/other cleartext values become absent without invalidating otherwise usable catalog data. The media live gate no longer depends on independently rate-limited Search. Anonymous live gates for lyrics/QRC, comments, MV, related Tracks, and new-song-backed guest media resolution all pass without persisting returned content. Repeated Search returned QQ result code `2001`, independently classified as rate limiting; it remains a truthful retryable service failure rather than an empty result. Rust format, 388 offline tests, strict Clippy, Dart format/analyze, and 23 targeted lyric/media Flutter tests pass. Exact outcome rules and evidence limits are recorded in `docs/research/qqmusic-read-availability-audit.md`; product runtime observation remains Human evidence.
- Maintainer-operated QQ Web QR debugging now proves the full approval and credential-exchange path succeeds. Redacted phase evidence showed `check_sig` and QQ Connect authorization returning redirects, followed by an HTTP-successful, syntactically valid musicu login envelope. The remaining failure was local: the decoder flattened every top-level musicu value into a login-result map, so scalar metadata such as timestamps and trace IDs made an otherwise valid named `QQLogin` result fail as `InvalidJson`. The decoder now reads only the global code and exact named login result; a synthetic regression includes unrelated scalar metadata, and the maintainer confirmed that the same build signed in after QR approval. The opt-in diagnostic records only phase/status/presence booleans and redacted error categories. Rust format, 388 offline tests, strict Clippy, Dart format/analyze, and 40 targeted authentication tests pass; full-process restore after this specific QQ login remains a separate maintainer observation until confirmed.
- The Android-wide data failure was traced below the QQ protocol layer: `reqwest` uses rustls platform verification, but the Android package neither bundled the verifier's JVM component nor initialized rustls with an Android application context before the first HTTPS request. The bounded fix packages the Cargo-matched local verifier artifact, keeps it through release shrinking, loads the existing Rust Bridge from `FuraApplication`, and initializes the verifier during process startup. Debug x64 and Release ARM64 APKs build; static APK inspection confirms the verifier class, JNI initializer, application class, permissions, signature, and target ABI; Rust format/test/strict Clippy plus Dart analyze and all 430 Flutter tests pass. A gated anonymous Android HTTPS integration regression is available, but physical-device execution remains required before claiming Android QQ data recovery.
- HD-018 formally names the product `fura music`; user-visible Flutter and platform display metadata now use that name while repository/package/binary, application-ID, Settings-key, and signing identifiers remain compatibility-preserving and HD-001 keeps those release choices pending. The wide desktop Sidebar now merges brand/account into one compact identity row, renders every personal Playlist in an independently clipped scroll viewport, keeps Settings pinned below that viewport, and routes into the existing typed theme/quality preferences without changing playback ownership. The clipping regression has a focused structural test, a 14-Playlist fixture proves the former seven-item truncation is gone, and canonical 1440×900 plus 390×844 renders show the candidate without overflow. Dart format/analyze, all 430 Flutter tests, and the Linux Release build pass; the Sidebar/Settings candidate remains pending Human visual review.
- The maintainer redirected the current Human-gated UI candidate to three bounded Shell interactions: retained primary destinations now use a Material-emphasized transition without rebuilding their local state; Playlist detail opens inside the adaptive authenticated Shell with Sidebar/rail, top region, persistent player, compact bottom navigation, and a shared dense Track-row visual grammar; and the desktop top Search field accepts text in place, then submits directly into the existing Track search surface. Canonical 1440×900 and 390×844 candidates render without overflow; Dart format/analyze and all 428 Flutter tests pass. The candidate remains pending maintainer visual/runtime review and does not self-accept the revised Shell behavior.
- HD-017 redirects the current Human-gated UI candidate to signed-out Home. The page now loads the existing anonymous QQ public-playlist feed and public `Latest` new-song collection instead of rendering account-only Daily/Radar/personalized sign-in bars. The hero remains public; a second public Playlist and the new-song collection fill the compact top recommendations; lower shelves are labeled `Popular playlists` and `New songs`, and new-song rows use the existing guest playback Queue path. Authenticated Daily 30, Radar, personalized Playlists/Tracks, and current-Track related listening are unchanged. Synthetic 1440×960 and 390×844 candidates render without overflow; Dart format/analyze, all 426 Flutter tests, and the Linux Release build pass. Live catalog availability and visual acceptance remain maintainer evidence.
- The maintainer explicitly switched execution to `HUMAN_GATED_REGRESSION` for five reproduced Liked-page, persistent-player, Expanded Now Playing, and synchronized-lyric presentation defects. Work is bounded to those regressions; the resulting visual candidate requires Human review and does not reopen unrelated Home, Shell, navigation, or Core architecture work.
- The favorite-Artist placeholder report exposed a missing supported data path rather than an image-cache failure: QQ Artist summaries previously stopped at MID/name, while Domain, Bridge, and Dart had no optional portrait field. The bounded candidate now validates the QQ MID, derives the independently cross-checked HTTPS `T001` singer portrait, preserves it through provider-neutral typed models, and renders it in favorite-Artist, Artist Search, and Artist detail surfaces through one shared fallback-safe component. Offline mapping/UI checks and a bounded public JPEG range request pass; an authenticated favorite-Artist visual rerun remains the Human acceptance evidence.
- The bounded candidate now uses one non-animated Liked-row hover owner that clears on scrolling, official secondary Material tabs with retained animated pages, a stable three-zone desktop player through normal resize, non-blocking artwork-color prefetch with an eight-entry `(artwork, brightness)` cache, a Material-emphasized Expanded Now Playing slide from and back toward the persistent player, truthful `Unavailable` handling for empty instrumental lyrics, and stable in-place lyric/translation rows whose last started line/word remains selected until the next begins. The persistent player restores its compact sequential transport/progress composition while retaining the resize-safe zone widths: 88 px total height, symmetric 8 px top/bottom insets, a 48 px primary action, and a non-overlapping 24 px progress row. Expanded Now Playing no longer hides Track or lyric content while extracting colors and does not repeat extraction after reopening the same cached artwork. Flutter format/analyze and all 424 tests pass; the Rust workspace's 389 offline tests, format, and strict Clippy pass, with eight live tests remaining ignored by default. Canonical desktop/mobile and transition renders remain Human-review evidence rather than visual acceptance. Exact QQ translation alignment beyond already mapped timestamps still requires sanitized live evidence and was not guessed.
- HD-016 Media Source Separation is implemented and machine-verified. Catalog/Account Provider capability ownership is distinct from the provider-neutral `MediaSourceResolver`/`MediaSourceCoordinator` path; production statically assembles only the QQ resolver, while a test-only synthetic resolver proves generic routing is not hardwired to `QQMusicProvider`. The public Bridge uses provider-neutral media names, and the generic Dart gateway no longer owns QQ vault policy; QQ authentication owns explicit QQ-rejection cleanup. This work did not add another service, cross-service matching, a plugin runtime, a marketplace, or a sidecar.
- The first-release Core-capability workstream checkpointed on 2026-08-28. Its audit has no required executable `MISSING` item; every remaining gap is exactly evidence-, environment-, or human-decision-blocked. See `docs/development/first-release-core-capability-checkpoint.md`.
- M2 through M6 are checkpointed. HD-013 redirects the active M7 visual-review surface to a bounded Home data-slot correction; Liked Songs and Expanded Now Playing remain implemented and unaccepted.
- M1 still lacks the maintainer-operated real QQ playback → Queue → synchronized/word-timed lyric observation. The checkpoint does not close or bypass it.
- Home no longer presents Popular Programs. `More from your listening` uses a distinct, cancellable current-Track related read rather than relabeling personalized Tracks. Public Feed evidence showed valid personalized playlists with `http://qpic.y.qq.com` artwork; the Client upgrades that and the other exact QQ image hosts now documented by the read-availability audit while still rejecting arbitrary cleartext artwork. The previous anonymous modern related request returned successful empty envelopes for bounded public seeds; the Client now uses the independently implemented signed route whose ignored live regression returned a non-empty bounded Track set. Offline and live protocol regressions pass; an authenticated Home rerun is still needed to confirm visible production recovery.
- The maintainer redirected the current UI task to keep the normal adaptive product Shell visible while signed out and present authentication in a modal. That modal offers QQ Web QR and the already verified WeChat QR path without collecting a password. Public Home/Discover/Search remain reachable; account-only Home and Library surfaces show explicit sign-in states instead of launching account reads. QQ QR bootstrap, polling, confirmed authorization, and credential exchange now have maintainer-operated live evidence; the signed-out Shell candidate still awaits desktop/mobile visual review.
- Guest media resolution no longer fails before transport. Signed-out playback now requests only anonymous M500 standard quality with `uin=0` and no Cookie or fabricated credential; a bounded live gate found a playable source among ten public Search results. Tracks for which QQ returns no anonymous source still offer sign-in without claiming a restriction reason. Authenticated high-to-standard fallback is unchanged.
- HD-014 now authorizes system playback over the existing single Flutter playback owner. One thin `audio_service` handler publishes provider-neutral current metadata, Queue mode and position, delegates system transport/seek/mode commands to the existing Rust-backed Queue/controller, and configures music audio focus plus interruption/noisy-device pause. A maintainer Linux run reproduced stale/non-seekable system progress plus unavailable shuffle/repeat controls: the pinned generic MPRIS adapter stored only one position sample and left those commands incomplete. Linux now uses a project-owned MPRIS edge with projected position, typed Track identity, absolute/relative seek, truthful capabilities, and bidirectional shuffle/repeat/volume. Unit regressions and a real session-bus integration pass; KDE/GNOME shell retest remains required. Other platform evidence is unchanged.
- HD-015 authorizes a manual GitHub Actions packaging gate for maintainer runtime testing. Run 33465045077 passed the offline quality gate plus Android, Windows, macOS, and iOS Simulator packages, and confirmed that the `ubuntu-24.04` Linux dependency-install step now succeeds. Linux then stopped in the MPRIS integration step before its Release build: that command first builds the complete native app, while the checked-in `media_kit_video` Linux CMake edge requires `pkg-config` libmpv and the resulting executable links `libmpv.so.2`; the workflow had installed GTK, libsecret, and GStreamer but omitted libmpv. The Linux job now installs Ubuntu's `libmpv-dev`, reports the resolved mpv version, and rejects unresolved `ldd` entries before upload. A new remote run and physical Android retest remain required; this local correction does not establish either result.

# Current Capability Boundary

- Desktop QQ quick login is a first-party local-client authorization candidate, not a stored-account import: discovery is user-triggered, raw identifiers and tickets remain Rust-owned, and authentication is not reported until QQ Connect and QQ Music exchange succeed. Android/iOS installed-client QQ/WeChat authorization remains externally blocked by application identity/signature/Universal-Link and QQ Music session-exchange requirements; generic HTTPS dispatch is not counted as that capability.
- Later UI can reuse typed Account, Home recommendation, Library read/mutation, Search, Discover, foreground playback/Queue/mode, Lyrics, Comments, MV, and Settings foundations without raw QQ responses or fabricated semantics.
- Popular Programs is excluded by HD-013. System playback is authorized by HD-014 without changing the single-player/Rust-Queue ownership. Playlist rename and Artist mutation remain protocol-evidence-blocked. Physical Android and Apple/Windows system-playback runtime claims remain environment-blocked.
- Remote-write foundations have independent protocol evidence and strict offline lifecycle coverage, but this repository did not mutate the maintainer's account. Confirmation, refresh, and maintainer-operated live acceptance remain later UI/evidence work.
- Personalized recommendation availability and quality are not authenticated-account verified. The authenticated personalized Track set and anonymous current-Track related set are distinct and may not substitute for each other.

# Current Scheduling

- Active regression acceptance: Human review of `/tmp/fura-ui-review-002dr3b6/run.sh`: inspect single-color rows, scroll the title into/out of the top-search slot, and enter/exit Settings plus an ordinary playlist to judge canvas continuity and motion. Canonical synthetic desktop/compact renders and targeted regression checks have passed. The preceding corrected QQ read has a Human success report. No further account automation or adjacent UI/Core work is scheduled.

# Pending Liked Pagination Acceptance

- The retained Human acceptance task is a maintainer-operated large-playlist scroll/search check on Liked and an ordinary playlist: scroll slowly then quickly through multiple former page boundaries, return upward and confirm content/position remain stable, then start a Liked query while another page is loading. Confirm exact/approximate results appear progressively, clear the query to stop full scanning, and test explicit retry if a real transient failure occurs. Report only perceived pauses, position stability, early-match behavior, and recovery; do not share playlist identity, Track title, account identifier, Cookie, or other private content. The prior Human check already confirms that the corrected authenticated path advances beyond 300 and reaches the reported 1,032-row total. This candidate retains loaded session metadata; it does not implement sparse arbitrary-page jumps, eviction, or cross-launch caching.

# Pending and Historical Scheduling Context

- Pending media-source acceptance: retry the reported official-client-playable Track with the rebuilt compatibility candidate. Report only whether full playback starts and the actual quality label; do not share media URL, account identifier, Cookie, vkey, or other credential material. The agent must not load the stored account from a CLI test.
- Pending Settings Shell review: confirm that entry/exit no longer flashes or reveals the retained page beneath the Settings controls, the medium Rail reads as one uninterrupted navigation group without a divider, and extended desktop sign-in/sign-out is naturally integrated into the top-left identity row with no duplicate top-right action. Automated structure checks and captured transition frames do not self-accept motion or placement.
- Pending Discover/player review: confirm the large heading/description and scrollable five-section tabs feel consistent with Liked, the tabs-only collapsed state and Shell title transition are stable, two-column compact Playlist cards and responsive desktop density are appropriate, and real scrolling content remains visible around the player capsule without making final pagination controls unreachable. Automated geometry, interaction, synthetic screenshots, and the bounded Material review do not self-accept these visual choices.
- Pending Liked-root review: confirm that no visible Library entry remains, Home/compact/rail/extended routes land on Liked, the medium Settings control sits naturally at bottom-left, missing built-in liked songs does not hide Playlists/Albums, and compact/desktop header collapse/expand feels stable. The removed favorite-Artist root and retained internal legacy code are explicit scope boundaries, not claims of data deletion.
- Pending persistent-player review: verify the first-play reveal, constant compact player/navigation height across destinations, MD3 tonal treatment, play/Queue hit isolation, artwork/title/background entry into Expanded Now Playing, and the Artist-line Album/Artist catalog affordance. Automated layout, semantics, transition, and screenshot checks do not self-accept the visual feel.
- Pending Settings hierarchy review: verify the shared navigation-slot replacement, Back placement, section labels, Settings search semantics, absence of account/login actions, retained player/context, and the perceptual quality of both entry and exit motion. Automated renders and transition assertions do not self-accept these visual choices; no adjacent Shell redesign begins while this candidate is pending.
- Desktop QQ Core direct authorization is now live-verified on this Linux host. Human acceptance still needs the rebuilt Fura application: with QQ signed in, open the sign-in dialog, choose QQ, confirm the expected nickname/masked hint and generic account icon appear beside the QQ QR, select it, confirm the dialog completes into authenticated Home, then perform a clean-process restart to verify secure restore. Retest QQ and WeChat QR fallback once. Do not capture or share a real QR, full QQ number, local ticket, cookie, signed URL, OAuth code, or credential. Mobile native work remains blocked as recorded in HD-022.
- The Home candidate needs maintainer visual/runtime review at desktop and compact widths: confirm that the public spotlight rotation feels appropriate, Daily/Radar remain independently recognizable, `Fresh releases` complements rather than displaces personalized songs, and the real account/public endpoints populate the same truthful slots. No adjacent page or further aesthetic iteration starts autonomously while this candidate is pending.
- The QQ read-availability regression is machine-verified. A maintainer runtime pass may now compare previously problematic lyric, playlist-artwork, MV, comment, and guest-playback cases: a missing optional surface must remain absent/unavailable without breaking the parent page, while retryable service failures must not masquerade as empty data. Search result code `2001` is an upstream rate limit and should be retried later rather than repeatedly probed. This does not close the separate authenticated recommendation, QR, platform, or M1 playback evidence.
- The Shell navigation/Playlist/Search candidate is implemented and machine-verified. The next action is maintainer review of primary-page motion, Playlist detail placement at desktop and compact widths, and the direct Track-search submission behavior. No adjacent page or further aesthetic correction starts autonomously while this candidate is pending.
- The signed-out Home public-recommendation candidate is implemented and machine-verified under HD-017. The next action is maintainer visual/runtime review of real anonymous public playlists and new songs; no adjacent page or further aesthetic correction starts autonomously while this candidate is pending.
- Media Source Separation has completed its bounded safe implementation scope. Core routing, the QQ-owned resolver, provider-neutral Bridge, QQ-owned vault-cleanup edge, synthetic routing proof, pinned Bridge generation, full Rust gate, full Flutter tests, and Linux native Bridge integration pass. No additional production source or adjacent feature is authorized; scheduling returns to the still-pending Human/environment/external evidence below without changing execution mode.
- The prior Liked collection-tabs candidate retains the approved dense table/list presentation for Songs; real user playlists are split by typed owned/saved semantics; favorite Albums reuse the existing typed collection; and unsupported program/video account collections remain explicit rather than being fabricated from playlists or Track-associated MV.
- Home still needs a maintainer authenticated rerun for Daily/personalized recommendation availability and the corrected personalized/related recommendation presentation. Liked Songs and Expanded Now Playing remain implemented and unaccepted.
- The Liked implementation intentionally omits unsupported download and batch-edit controls. Program and video tabs expose the requested information architecture, but real collection content remains capability-blocked; synthetic review content does not enter production and no account data was accessed.
- No other page may begin before maintainer visual acceptance or another explicit Human redirect. Automated checks and the bounded Material 3 review cannot supply that acceptance.
- Do not automate stored-account access, real-account mutation acceptance, or secret-bearing screenshots/fixtures.
- QQ Web QR approval and credential exchange passed a maintainer-operated compatibility observation without retaining account material. A clean-process restore after this specific QQ login remains to be confirmed; the removed phone/SMS path is no longer a product capability or pending acceptance item.
- System playback now needs bounded maintainer/runtime checks: Android notification/lock-screen controls, media buttons, audio focus and task-background continuity; Linux KDE/GNOME should recheck that progress advances, scrubbing seeks the current Track, and shell shuffle/repeat round-trip to the app; and later iOS/macOS Control Center plus Windows SMTC when those hosts are available. Platform gaps must remain separate rather than being inferred from another target.
- Pending Android validation is to install a newly produced ARM64 APK and run the bounded anonymous HTTPS regression (or equivalently confirm signed-out Search/Home data loads) on a physical target. Only that target observation can close the Android rustls/network regression; broader account and system-media testing remains separate. The packaging workflow should then be rerun after the fix is pushed, preserving each job's exact result. A failed platform job is a target-specific build regression, not evidence that another artifact works or that the whole project is release-ready.

# Blockers

- **M1 evidence:** maintainer-operated ordinary QQ Track playback → Queue navigation → synchronized lyrics → word timing.
- **Protocol evidence:** playlist rename and Artist follow/unfollow need independent current request/success evidence before Client work.
- **Target environments:** physical Android and Apple/Windows secure-storage/system-media behavior cannot be established on this host. Windows SMTC timeline/seek also remains limited by TD-008.
- **Release:** production identity, signing, external distribution, and native-video notices remain blocked by HD-001 and TD-002/TD-006.

# Pending Human Decisions

- **HD-001:** platform application identifiers, signing custody, and distribution ownership.

# Important Evidence Limits

- Static inspection of the current QQ login script, a loopback TLS check, and the explicitly authorized 2026-09-11 content-free live test prove one successful Linux Core quick-login exchange for the sole local account. Synthetic tests prove current/legacy parsing, strict signed-redirect validation, redaction, selection, request construction, cancellation, and credential installation rules. This does not prove visible Flutter dialog completion, credential restoration after a clean application restart, QR fallback behavior, multiple-account selection, or Windows/macOS compatibility.
- The 2026-09-01 anonymous live gates prove compatibility only for their bounded public samples. They do not prove that every QQ Track owns lyrics, comments, MV, artwork, related Tracks, or a guest-playable source, and they do not justify labeling a generic unavailable result as VIP, region, copyright, or device restriction. Search code `2001` was independently identified as rate limiting, not a zero-result catalog response.
- Offline and Widget tests prove implemented rules and retained presentation behavior, not current authenticated QQ CDN playback, personalized recommendation quality, or broad live catalog compatibility.
- Linux local media, packaged Bridge, and development builds do not prove physical-device audio focus, hardware video decode, unavailable operating systems, or release readiness.
- The locally rebuilt ARM64 Release APK proves the final package declares Internet access, contains the ARM64 Rust bridge plus Android certificate-verifier code, and exposes the startup initializer. It does not prove physical-device DNS/TLS reachability, current QQ compatibility, authenticated behavior, or that a future remote packaging run will pass every target.
- Linux MPRIS unit and real session-bus integrations prove service registration, property signatures, progressing position calculation, Track-bound absolute/relative seek dispatch, and shuffle/repeat round trips through the existing handler. They do not prove a particular KDE/GNOME shell renders or invokes every supported control correctly. Android packaging still does not prove lock-screen/notification behavior, task-removal continuity, or headset controls; Apple/Windows runtime behavior remains unverified.
- Home, Liked Songs, and Expanded Now Playing remain unaccepted visual candidates; canonical synthetic renders and automated tests do not prove Human visual acceptance, current authenticated personalization availability, or recommendation quality across the catalog.
- Historical research/checkpoint documents remain evidence snapshots. Current scheduling is governed by `AGENTS.md`, `ROADMAP.md`, and this file.
